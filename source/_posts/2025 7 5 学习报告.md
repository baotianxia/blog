---
title: 2025 7 5 学习报告
date: 2025-07-05
tags:
- 学习报告
mathjax: true
---
# 2025 7 5 学习报告

考试

## [加分二叉树](http://cogs.pro:8081/cogs/problem/problem.php?pid=pySyyykae)

首先考虑树形dp，发现不可行，进行修改，最终写了个递归区间dp，时间复杂度O(n^2)，空间复杂度O(n^2)，通过。

本质上写的就是区间dp，枚举中转点（即这个区间形成的子树的根），由中序遍历的性质，区间左端点到中转点的子树的根是左子树，区间中转点到右端点的子树的根是右子树。于是按要求处理即可

```cpp
#include <iostream>
#include <algorithm>
#include <cstdio>
#define ll long long
using namespace std;
int n;
int fs[35];
ll f[35][35];
int rt[35][35];
ll dfs(int l,int r){
    if(l>r) return 1;
    if(l==r){
        return fs[l];
    }
    if(f[l][r]) return f[l][r];
    int t;
    for(int i=l;i<=r;i++){
        t=dfs(l,i-1)*dfs(i+1,r)+fs[i];
        if(t>f[l][r]){
            f[l][r]=t;
            rt[l][r]=i;
        }
    }
    return f[l][r];
}
void pr(int l,int r){
    if(l>r) return;
    if(l==r){
        cout<<l<<" ";
        return;
    }
    cout<<rt[l][r]<<" ";
    pr(l,rt[l][r]-1);
    pr(rt[l][r]+1,r);
}
int main(){
    freopen("jfecs.in","r",stdin); 
    freopen("jfecs.out","w",stdout); 
    cin>>n;
    for(int i=1;i<=n;i++){
        cin>>fs[i];
    }
    cout<<dfs(1,n)<<endl;
//    for(int i=1;i<=n;i++){
//        for(int j=1;j<=n;j++){
//            cout<<rt[i][j]<<" ";
//        }
//        cout<<endl;
//    }
    pr(1,n);
    return 0;
}
```

## [环路运输](http://cogs.pro:8081/cogs/problem/problem.php?pid=vyyNxVePU)

正解是区间dp+单调队列

![think](https://image-e0d.pages.dev/2025-07-07-20-08-10-a12b94d908bcf02155f49b9e8e13b9db-939e.png)

```cpp
#include <iostream>
#include <cstdio>
#include <algorithm>
#include <cmath> 
#include <deque> 
#include <cstring>
#define ll long long
using namespace std;
int n;
int a[2000006];
int main(){
// freopen("transportt.in","r",stdin);
// freopen("transportt.out","w",stdout);
 cin>>n;
 for(int i=1;i<=n;i++){
  cin>>a[i];
  a[i+n]=a[i];
 }
 ll ans=-999;
 deque<int>d;
 d.push_back(1);
 for(int i=2;i<=2*n;i++){
  while(!d.empty()&&d.front()<i-n/2) d.pop_front();
  ans=max(ans,(ll)i-d.front()+a[i]+a[d.front()]);
  while(!d.empty()&&a[d.back()]-d.back()<=a[i]-i) d.pop_back();
  d.push_back(i);
 }
 cout<<ans;
 return 0;
}

```

## [战略游戏](http://cogs.pro:8081/cogs/problem/problem.php?pid=vSJiiVPPW)

树形dp

如果父节点有士兵，则子节点可有可无  
如果父节点没有士兵。则子节点必须有

```cpp
#include <iostream>
#include <algorithm>
#include <cstdio>
#include <cstring>
using namespace std;
const int N=1503;
int head[N],nxt[2*N],to[2*N],num;
void add(int x,int y){
 num++;
 to[num]=y;
 nxt[num]=head[x];
 head[x]=num;
}
int f[N][2];
void dfs(int x,int fa){
 f[x][0]=0;
 f[x][1]=1;
 for(int i=head[x];i;i=nxt[i]){
  if(fa==to[i]) continue;
  dfs(to[i],x);
  f[x][0]+=f[to[i]][1];
  f[x][1]+=min(f[to[i]][0],f[to[i]][1]);
 }
 if(f[x][0]>=0x3f3f3f3f-1) f[x][0]=0;
 if(f[x][1]>=0x3f3f3f3f-1) f[x][1]=1;
}
int main(){
 freopen("strategic.in","r",stdin);
 freopen("strategic.out","w",stdout);
 int n;
 while(cin>>n){
  num=0;
  memset(f,0x3f,sizeof(f));
  memset(head,0,sizeof(head));
  for(int i=1;i<=n;i++){
   int x,cnt,y;
   scanf("%d:(%d)",&x,&cnt) ;
   for(int j=1;j<=cnt;j++){
    cin>>y;
    add(x+1,y+1);
    add(y+1,x+1);
   }
  }
  dfs(1,0);
  cout<<min(f[1][0],f[1][1])<<endl;
 }
}

```

## [方块消除](http://cogs.pro:8081/cogs/problem/problem.php?pid=viXXiVajq)

## [外卖](http://cogs.pro:8081/cogs/problem/problem.php?pid=vNQNJqaUg)
